= form_for([@post, @post.comments.build]) do |f| #Post.comments.build generador de un arreglo para el comentario
  %p
    = f.label :commenter
    %br
    = f.text_field :commenter
  %p
    = f.label :body
    %br
    = f.text_area :body
  %p
    = f.submit